// Function parameter
function tyrIt(number){
    let result = number * 2;
    return result;
}

var fast = tyrIt(5)
var second = tyrIt(10);
var third = tyrIt(20);
total = fast + second + third;
console.log(total);
// Multiple Parameter in Function
function add(num1, num2, num3){
    var result = num1 + num2 + num3;
    return result;
}
var total = add(34, 22, 56);
console.log(total);

// javaScript
function greet(name1, lastName){
    var result = name1, lastName;
    return result;
}
 let fast = greet('yeasin','arafat');
let second = greet('Mohammad','rakib');
 let allName = fast + ' ' + second;
 console.log(allName);

 // functoin
 
const name1 = [
]
function addall(){
for (let i = 0; i < name.length; i++) {
    const  = name[i];
    
}

}
